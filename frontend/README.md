# KantinTracker — frontend

Quick start (frontend):

1. Install dependencies:

```bash
cd frontend
npm install
```

2. Run dev server:

```bash
npm run dev
```

Default expects backend at `http://localhost:5000/api`. You can change this by setting `VITE_API_URL` in your environment.
